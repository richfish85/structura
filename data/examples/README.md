# Example data

The records in this folder are fictional format demonstrations. They must never be imported or described as historical hardware facts.

Real research begins in `data/intake/` and follows the staged review workflow in `docs/research-workflow.md`.
