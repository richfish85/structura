# Independent connected-object audit — 2026-09-09

This is an audit submission, not approval or canonical promotion. I changed only this audit run folder.

The packet itself substantially respects the verifier and proposed ontology. The memory/controller entries identify shared constituent descriptions rather than specific packages, and the comparison preserves manufacturer and TurboWrite limits. The most material problem is the import gate: supported keys can retain their verdict while their factual content changes. Six isolated in-memory mutation probes passed, including changing a relationship to a different endpoint, a false comparison and a false diagram constituent. Details and bounded remedies are in audit.json.

The current snapshot was opened read-only. It has 22 relationships and no canonical entities, and SQLite integrity_check returned ok. I read the display implementation; no browser usability or layout judgment is claimed.

The 1997 repairs are faithful to the original research assertions. CPU/GPU changes remove a spurious leading comma and map vocabulary. HDD additionally restores the malformed source columns for hdd-aud-002. Row counts, dispositions and authors are retained, all file hashes match, and every parsed delta appears accurately in its repair manifest. Mapping resolved scope checks to scope_error is awkward vocabulary but does not create an open finding because resolved_no_change is retained and displayed.

The entity evidence needs to be visible in the component journey. Generic PCIe identity descriptions should also avoid device-specific clauses supported only by a generic PCI-SIG webinar; the correct device source already exists on the relationship.
