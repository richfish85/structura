# CPU Omission Normalization Notes

Covered exactly the 12 frozen omission-derived keys. Quarter precision is retained for AMD-K6-2 366/380 and mobile AMD-K6 300. Intel OverDrive 300/333 remain separate input keys but are both `sku_unresolved` and `review_required` because the verifier found one singular OverDrive product with two output mappings. Xeon 450 is `review_required` because the announcement and planned shipment do not settle the qualifying-event boundary. Mobile, upgrade, desktop, and server/workstation segments remain explicit in market notes.

No existing ten-row normalization artifact, verifier artifact, database, or canonical record was changed.
